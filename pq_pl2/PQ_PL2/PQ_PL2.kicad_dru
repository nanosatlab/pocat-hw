#(version 2)

# Clearance for 50 ohm RF pads 
#(rule "RF Tracks Clearance"
#(condition "A.Type == 'Zone' && B.Type == 'Pad' && A.NetClass == '50ohm'")
#  (constraint clearance (min 6mil)))

# Clearance for 75 ohm RF pads 
#(rule "RF Tracks Clearance"
#  (condition "A.Type == 'Zone' && B.Type == 'Pad' && A.NetClass == '75ohm'")
#  (constraint clearance (min 6mil)))